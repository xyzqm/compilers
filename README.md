To run the code, modify `src.pas` and run the following command:

```
java Main.java src.pas && java -jar Mars4_5.jar out.asm
```

# Design Notes

Variables are locally scoped to blocks defined by `begin` and `end`.
